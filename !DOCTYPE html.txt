<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Rajat & Priyanka — 25 June 2026</title>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Cinzel:wght@400;600&family=Lato:wght@300;400&display=swap" rel="stylesheet"/>
  <style>
    *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

    :root {
      --burgundy: #6B1F2A;
      --gold: #C9973A;
      --gold-light: #E8C06A;
      --cream: #FDF6EC;
      --dark: #1A0A0F;
      --soft: #F5EAD8;
    }

    html { scroll-behavior: smooth; }

    body {
      background: var(--dark);
      color: var(--cream);
      font-family: 'Lato', sans-serif;
      font-weight: 300;
      overflow-x: hidden;
    }

    /* ── HERO ── */
    .hero {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      position: relative;
      text-align: center;
      padding: 2rem;
      overflow: hidden;
      background: radial-gradient(ellipse at 50% 30%, #3d0f1a 0%, #1A0A0F 65%);
    }

    /* Mandala background */
    .mandala {
      position: absolute;
      width: 700px;
      height: 700px;
      opacity: 0.06;
      top: 50%; left: 50%;
      transform: translate(-50%, -50%);
      animation: rotate 60s linear infinite;
    }

    @keyframes rotate { to { transform: translate(-50%, -50%) rotate(360deg); } }

    /* Corner ornaments */
    .corner {
      position: absolute;
      width: 160px;
      height: 160px;
      opacity: 0.5;
    }
    .corner svg { width: 100%; height: 100%; }
    .corner--tl { top: 20px; left: 20px; }
    .corner--tr { top: 20px; right: 20px; transform: scaleX(-1); }
    .corner--bl { bottom: 20px; left: 20px; transform: scaleY(-1); }
    .corner--br { bottom: 20px; right: 20px; transform: scale(-1); }

    /* Gold line */
    .divider {
      width: 200px;
      height: 1px;
      background: linear-gradient(to right, transparent, var(--gold), transparent);
      margin: 1.2rem auto;
    }
    .divider-sm {
      width: 80px;
      height: 1px;
      background: linear-gradient(to right, transparent, var(--gold-light), transparent);
      margin: 0.8rem auto;
    }

    .hero-label {
      font-family: 'Cinzel', serif;
      letter-spacing: 0.35em;
      font-size: 0.7rem;
      color: var(--gold);
      text-transform: uppercase;
      opacity: 0;
      animation: fadeUp 1s 0.3s forwards;
    }

    .hero-names {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(4rem, 12vw, 8.5rem);
      font-weight: 300;
      line-height: 1;
      color: var(--cream);
      opacity: 0;
      animation: fadeUp 1.1s 0.7s forwards;
    }

    .hero-names .amp {
      font-style: italic;
      color: var(--gold);
      font-size: 0.75em;
    }

    .hero-date {
      font-family: 'Cinzel', serif;
      font-size: clamp(0.85rem, 2vw, 1.1rem);
      letter-spacing: 0.25em;
      color: var(--gold-light);
      opacity: 0;
      animation: fadeUp 1s 1.1s forwards;
    }

    .hero-sub {
      font-family: 'Cormorant Garamond', serif;
      font-style: italic;
      font-size: 1.15rem;
      color: rgba(253,246,236,0.6);
      opacity: 0;
      animation: fadeUp 1s 1.4s forwards;
    }

    .scroll-hint {
      position: absolute;
      bottom: 2.5rem;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 6px;
      opacity: 0;
      animation: fadeIn 1s 2s forwards;
    }
    .scroll-hint span {
      font-family: 'Cinzel', serif;
      font-size: 0.6rem;
      letter-spacing: 0.3em;
      color: var(--gold);
      opacity: 0.7;
    }
    .scroll-dot {
      width: 1px;
      height: 40px;
      background: linear-gradient(to bottom, var(--gold), transparent);
      animation: scrollPulse 2s ease-in-out infinite;
    }
    @keyframes scrollPulse {
      0%,100% { opacity: 0.3; transform: scaleY(1); }
      50% { opacity: 1; transform: scaleY(1.15); }
    }

    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(24px); }
      to   { opacity: 1; transform: translateY(0); }
    }
    @keyframes fadeIn {
      from { opacity: 0; }
      to   { opacity: 1; }
    }

    /* ── SECTION BASE ── */
    section {
      padding: 6rem 2rem;
      text-align: center;
    }

    .section-tag {
      font-family: 'Cinzel', serif;
      font-size: 0.65rem;
      letter-spacing: 0.4em;
      color: var(--gold);
      text-transform: uppercase;
      margin-bottom: 0.5rem;
    }

    .section-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(2.5rem, 6vw, 4rem);
      font-weight: 300;
      color: var(--cream);
      line-height: 1.1;
    }

    .section-title em {
      font-style: italic;
      color: var(--gold-light);
    }

    /* ── COUNTDOWN ── */
    #countdown {
      background: linear-gradient(180deg, #1A0A0F 0%, #2a0d17 50%, #1A0A0F 100%);
    }

    .countdown-grid {
      display: flex;
      justify-content: center;
      gap: clamp(1.5rem, 4vw, 3.5rem);
      margin-top: 3rem;
      flex-wrap: wrap;
    }

    .countdown-block {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 0.4rem;
    }

    .countdown-num {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(3.5rem, 9vw, 6rem);
      font-weight: 300;
      color: var(--cream);
      line-height: 1;
      min-width: 2ch;
      position: relative;
    }

    .countdown-num::after {
      content: '';
      position: absolute;
      bottom: 0; left: 50%;
      transform: translateX(-50%);
      width: 40px; height: 1px;
      background: var(--gold);
    }

    .countdown-label {
      font-family: 'Cinzel', serif;
      font-size: 0.6rem;
      letter-spacing: 0.3em;
      color: var(--gold);
      margin-top: 0.6rem;
    }

    .countdown-sep {
      font-family: 'Cormorant Garamond', serif;
      font-size: 4rem;
      color: var(--gold);
      opacity: 0.3;
      line-height: 1;
      align-self: flex-start;
      margin-top: 0.3rem;
    }

    /* ── STORY ── */
    #story {
      background: var(--dark);
      max-width: 860px;
      margin: 0 auto;
    }

    .story-text {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(1.15rem, 2.5vw, 1.45rem);
      font-weight: 300;
      line-height: 1.9;
      color: rgba(253,246,236,0.75);
      margin-top: 2.5rem;
    }

    .story-text em {
      font-style: italic;
      color: var(--gold-light);
    }

    /* ── EVENTS ── */
    #events {
      background: linear-gradient(180deg, #1A0A0F 0%, #200c14 100%);
    }

    .events-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 1.5rem;
      max-width: 1000px;
      margin: 3rem auto 0;
    }

    .event-card {
      border: 1px solid rgba(201,151,58,0.25);
      padding: 2.5rem 2rem;
      position: relative;
      overflow: hidden;
      transition: border-color 0.3s, transform 0.3s;
    }
    .event-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: linear-gradient(135deg, rgba(201,151,58,0.05) 0%, transparent 60%);
      pointer-events: none;
    }
    .event-card:hover {
      border-color: rgba(201,151,58,0.6);
      transform: translateY(-4px);
    }

    .event-icon {
      font-size: 2rem;
      margin-bottom: 1rem;
      display: block;
    }

    .event-name {
      font-family: 'Cinzel', serif;
      font-size: 0.85rem;
      letter-spacing: 0.25em;
      color: var(--gold);
      margin-bottom: 0.5rem;
    }

    .event-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: 1.8rem;
      color: var(--cream);
      margin-bottom: 1rem;
    }

    .event-detail {
      font-size: 0.85rem;
      color: rgba(253,246,236,0.55);
      line-height: 1.7;
      letter-spacing: 0.04em;
    }

    /* ── GALLERY ── */
    #gallery {
      background: var(--dark);
    }

    .gallery-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      grid-template-rows: 200px 200px;
      gap: 6px;
      max-width: 900px;
      margin: 3rem auto 0;
    }

    .gallery-cell {
      overflow: hidden;
      position: relative;
    }

    .gallery-cell:first-child {
      grid-row: 1 / 3;
    }

    .gallery-inner {
      width: 100%; height: 100%;
      background: linear-gradient(135deg, #2a0d17, #3d1525);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2.5rem;
      transition: transform 0.5s ease;
    }
    .gallery-cell:hover .gallery-inner { transform: scale(1.05); }

    /* ── RSVP ── */
    #rsvp {
      background: linear-gradient(180deg, #200c14 0%, #1A0A0F 100%);
    }

    .rsvp-form {
      max-width: 520px;
      margin: 3rem auto 0;
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }

    .rsvp-input {
      background: transparent;
      border: 1px solid rgba(201,151,58,0.3);
      color: var(--cream);
      font-family: 'Lato', sans-serif;
      font-size: 0.9rem;
      letter-spacing: 0.04em;
      padding: 1rem 1.2rem;
      outline: none;
      transition: border-color 0.3s;
      width: 100%;
    }
    .rsvp-input::placeholder { color: rgba(253,246,236,0.3); }
    .rsvp-input:focus { border-color: var(--gold); }

    select.rsvp-input option { background: #1A0A0F; }

    .rsvp-btn {
      background: transparent;
      border: 1px solid var(--gold);
      color: var(--gold);
      font-family: 'Cinzel', serif;
      font-size: 0.75rem;
      letter-spacing: 0.35em;
      text-transform: uppercase;
      padding: 1.1rem 2rem;
      cursor: pointer;
      transition: background 0.3s, color 0.3s;
      margin-top: 0.5rem;
    }
    .rsvp-btn:hover { background: var(--gold); color: var(--dark); }

    .rsvp-success {
      display: none;
      font-family: 'Cormorant Garamond', serif;
      font-style: italic;
      font-size: 1.4rem;
      color: var(--gold-light);
      margin-top: 1rem;
    }

    /* ── FOOTER ── */
    footer {
      text-align: center;
      padding: 4rem 2rem;
      background: var(--dark);
      border-top: 1px solid rgba(201,151,58,0.15);
    }

    .footer-names {
      font-family: 'Cormorant Garamond', serif;
      font-size: 2.5rem;
      color: var(--cream);
      font-weight: 300;
      margin-bottom: 0.5rem;
    }
    .footer-names .amp { color: var(--gold); font-style: italic; }

    .footer-note {
      font-size: 0.8rem;
      color: rgba(253,246,236,0.3);
      letter-spacing: 0.06em;
      margin-top: 1.5rem;
    }

    /* Responsive */
    @media (max-width: 600px) {
      .form-row { grid-template-columns: 1fr; }
      .gallery-grid { grid-template-columns: 1fr 1fr; grid-template-rows: auto; }
      .gallery-cell:first-child { grid-row: auto; }
      .corner { width: 80px; height: 80px; }
    }
  </style>
</head>
<body>

<!-- ══════════ HERO ══════════ -->
<section class="hero">

  <!-- Mandala SVG -->
  <svg class="mandala" viewBox="0 0 300 300" fill="none" xmlns="http://www.w3.org/2000/svg">
    <circle cx="150" cy="150" r="140" stroke="#C9973A" stroke-width="0.5"/>
    <circle cx="150" cy="150" r="110" stroke="#C9973A" stroke-width="0.5"/>
    <circle cx="150" cy="150" r="80" stroke="#C9973A" stroke-width="0.5"/>
    <circle cx="150" cy="150" r="50" stroke="#C9973A" stroke-width="0.5"/>
    <circle cx="150" cy="150" r="20" stroke="#C9973A" stroke-width="0.5"/>
    <g stroke="#C9973A" stroke-width="0.4">
      <line x1="150" y1="10" x2="150" y2="290"/>
      <line x1="10" y1="150" x2="290" y2="150"/>
      <line x1="51" y1="51" x2="249" y2="249"/>
      <line x1="249" y1="51" x2="51" y2="249"/>
      <line x1="150" y1="10" x2="249" y2="249"/>
      <line x1="150" y1="10" x2="51" y2="249"/>
      <line x1="10" y1="150" x2="249" y2="51"/>
      <line x1="10" y1="150" x2="249" y2="249"/>
      <line x1="290" y1="150" x2="51" y2="51"/>
      <line x1="290" y1="150" x2="51" y2="249"/>
    </g>
    <!-- Petal shapes -->
    <ellipse cx="150" cy="80" rx="12" ry="28" fill="#C9973A" opacity="0.15"/>
    <ellipse cx="150" cy="220" rx="12" ry="28" fill="#C9973A" opacity="0.15"/>
    <ellipse cx="80" cy="150" rx="28" ry="12" fill="#C9973A" opacity="0.15"/>
    <ellipse cx="220" cy="150" rx="28" ry="12" fill="#C9973A" opacity="0.15"/>
    <ellipse cx="100" cy="100" rx="12" ry="28" transform="rotate(45 100 100)" fill="#C9973A" opacity="0.1"/>
    <ellipse cx="200" cy="200" rx="12" ry="28" transform="rotate(45 200 200)" fill="#C9973A" opacity="0.1"/>
    <ellipse cx="200" cy="100" rx="12" ry="28" transform="rotate(-45 200 100)" fill="#C9973A" opacity="0.1"/>
    <ellipse cx="100" cy="200" rx="12" ry="28" transform="rotate(-45 100 200)" fill="#C9973A" opacity="0.1"/>
    <circle cx="150" cy="150" r="8" fill="#C9973A" opacity="0.3"/>
  </svg>

  <!-- Corner Ornaments -->
  <div class="corner corner--tl">
    <svg viewBox="0 0 100 100" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M5 5 L50 5 L50 15 L15 15 L15 50 L5 50 Z" stroke="#C9973A" stroke-width="0.8" fill="none"/>
      <path d="M5 5 L30 5" stroke="#C9973A" stroke-width="1.5"/>
      <path d="M5 5 L5 30" stroke="#C9973A" stroke-width="1.5"/>
      <circle cx="5" cy="5" r="3" fill="#C9973A"/>
      <path d="M20 5 Q35 20 20 35 Q5 20 20 5Z" fill="#C9973A" opacity="0.25"/>
      <line x1="50" y1="15" x2="50" y2="5" stroke="#C9973A" stroke-width="0.6"/>
      <line x1="15" y1="50" x2="5" y2="50" stroke="#C9973A" stroke-width="0.6"/>
    </svg>
  </div>
  <div class="corner corner--tr"><svg viewBox="0 0 100 100" fill="none"><path d="M5 5 L50 5 L50 15 L15 15 L15 50 L5 50 Z" stroke="#C9973A" stroke-width="0.8" fill="none"/><path d="M5 5 L30 5" stroke="#C9973A" stroke-width="1.5"/><path d="M5 5 L5 30" stroke="#C9973A" stroke-width="1.5"/><circle cx="5" cy="5" r="3" fill="#C9973A"/><path d="M20 5 Q35 20 20 35 Q5 20 20 5Z" fill="#C9973A" opacity="0.25"/></svg></div>
  <div class="corner corner--bl"><svg viewBox="0 0 100 100" fill="none"><path d="M5 5 L50 5 L50 15 L15 15 L15 50 L5 50 Z" stroke="#C9973A" stroke-width="0.8" fill="none"/><path d="M5 5 L30 5" stroke="#C9973A" stroke-width="1.5"/><path d="M5 5 L5 30" stroke="#C9973A" stroke-width="1.5"/><circle cx="5" cy="5" r="3" fill="#C9973A"/><path d="M20 5 Q35 20 20 35 Q5 20 20 5Z" fill="#C9973A" opacity="0.25"/></svg></div>
  <div class="corner corner--br"><svg viewBox="0 0 100 100" fill="none"><path d="M5 5 L50 5 L50 15 L15 15 L15 50 L5 50 Z" stroke="#C9973A" stroke-width="0.8" fill="none"/><path d="M5 5 L30 5" stroke="#C9973A" stroke-width="1.5"/><path d="M5 5 L5 30" stroke="#C9973A" stroke-width="1.5"/><circle cx="5" cy="5" r="3" fill="#C9973A"/><path d="M20 5 Q35 20 20 35 Q5 20 20 5Z" fill="#C9973A" opacity="0.25"/></svg></div>

  <p class="hero-label">We are getting married</p>
  <div class="divider-sm" style="margin-top:1.2rem;opacity:0;animation:fadeIn 1s 0.6s forwards;"></div>

  <h1 class="hero-names">
    Rajat<br/>
    <span class="amp">&amp;</span><br/>
    Priyanka
  </h1>

  <div class="divider" style="opacity:0;animation:fadeIn 1s 1s forwards;"></div>
  <p class="hero-date">25 &nbsp;·&nbsp; June &nbsp;·&nbsp; 2026</p>
  <div class="divider-sm" style="opacity:0;animation:fadeIn 1s 1.3s forwards;"></div>
  <p class="hero-sub">Together forever begins here</p>

  <div class="scroll-hint">
    <span>Scroll</span>
    <div class="scroll-dot"></div>
  </div>
</section>

<!-- ══════════ COUNTDOWN ══════════ -->
<section id="countdown">
  <p class="section-tag">The Big Day Approaches</p>
  <h2 class="section-title">Counting Down <em>with Joy</em></h2>
  <div class="divider" style="margin-top:1.5rem;"></div>

  <div class="countdown-grid">
    <div class="countdown-block">
      <div class="countdown-num" id="cd-days">--</div>
      <div class="countdown-label">Days</div>
    </div>
    <div class="countdown-sep">·</div>
    <div class="countdown-block">
      <div class="countdown-num" id="cd-hours">--</div>
      <div class="countdown-label">Hours</div>
    </div>
    <div class="countdown-sep">·</div>
    <div class="countdown-block">
      <div class="countdown-num" id="cd-mins">--</div>
      <div class="countdown-label">Minutes</div>
    </div>
    <div class="countdown-sep">·</div>
    <div class="countdown-block">
      <div class="countdown-num" id="cd-secs">--</div>
      <div class="countdown-label">Seconds</div>
    </div>
  </div>
</section>

<!-- ══════════ STORY ══════════ -->
<section id="story" style="max-width:100%;">
  <div style="max-width:780px;margin:0 auto;">
    <p class="section-tag">Our Story</p>
    <h2 class="section-title">Two Souls,<br/><em>One Destiny</em></h2>
    <div class="divider" style="margin-top:1.5rem;"></div>
    <p class="story-text">
      What began as a chance encounter blossomed into a love story written in the stars.
      <em>Rajat</em> and <em>Priyanka</em> found in each other not just a partner,
      but a best friend, a confidant, and a home.
      Through laughter and quiet moments, through adventures and ordinary days —
      every heartbeat has led to this one beautiful promise.
    </p>
    <p class="story-text" style="margin-top:1.5rem;">
      On the 25th of June, 2026, surrounded by the people they love most,
      they will say their vows and begin the greatest chapter yet.
      <em>You are invited to witness and celebrate their forever.</em>
    </p>
  </div>
</section>

<!-- ══════════ EVENTS ══════════ -->
<section id="events">
  <p class="section-tag">Celebrations</p>
  <h2 class="section-title">Join Us for <em>Every Moment</em></h2>
  <div class="divider" style="margin-top:1.5rem;"></div>

  <div class="events-grid">
    <div class="event-card">
      <span class="event-icon">🪔</span>
      <p class="event-name">Evening · 23 June 2026</p>
      <h3 class="event-title">Mehndi &amp; Sangeet</h3>
      <p class="event-detail">An evening of music, dance, and colour.<br/>Henna, laughter, and celebrations.</p>
    </div>
    <div class="event-card">
      <span class="event-icon">🌸</span>
      <p class="event-name">Morning · 24 June 2026</p>
      <h3 class="event-title">Haldi Ceremony</h3>
      <p class="event-detail">A turmeric-blessed ritual with<br/>family and close friends.</p>
    </div>
    <div class="event-card" style="border-color:rgba(201,151,58,0.5);">
      <span class="event-icon">💍</span>
      <p class="event-name">25 June 2026 · 7:00 PM</p>
      <h3 class="event-title">The Wedding</h3>
      <p class="event-detail">The main ceremony — where two hearts<br/>become one, forever.</p>
    </div>
    <div class="event-card">
      <span class="event-icon">🥂</span>
      <p class="event-name">25 June 2026 · 9:00 PM</p>
      <h3 class="event-title">Reception</h3>
      <p class="event-detail">Dinner, dancing, and memories that<br/>will last a lifetime.</p>
    </div>
  </div>
</section>

<!-- ══════════ GALLERY ══════════ -->
<section id="gallery">
  <p class="section-tag">Memories</p>
  <h2 class="section-title">A Glimpse of <em>Our Love</em></h2>
  <div class="divider" style="margin-top:1.5rem;"></div>

  <div class="gallery-grid">
    <div class="gallery-cell">
      <div class="gallery-inner" style="background:linear-gradient(135deg,#3d0f1a,#6b1f2a);">
        <span style="font-size:3.5rem;">💕</span>
      </div>
    </div>
    <div class="gallery-cell">
      <div class="gallery-inner" style="background:linear-gradient(135deg,#2a0d17,#4a1520);">
        <span style="font-size:2.5rem;">🌹</span>
      </div>
    </div>
    <div class="gallery-cell">
      <div class="gallery-inner" style="background:linear-gradient(135deg,#1f0a0f,#3d1525);">
        <span style="font-size:2.5rem;">🪷</span>
      </div>
    </div>
    <div class="gallery-cell">
      <div class="gallery-inner" style="background:linear-gradient(135deg,#2d1010,#5a1c2a);">
        <span style="font-size:2.5rem;">✨</span>
      </div>
    </div>
    <div class="gallery-cell">
      <div class="gallery-inner" style="background:linear-gradient(135deg,#1a0a0f,#2a0d17);">
        <span style="font-size:2.5rem;">🪢</span>
      </div>
    </div>
  </div>
  <p style="font-family:'Cormorant Garamond',serif;font-style:italic;color:rgba(253,246,236,0.35);margin-top:1.5rem;font-size:0.95rem;">Gallery coming soon — our favourite moments</p>
</section>

<!-- ══════════ RSVP ══════════ -->
<section id="rsvp">
  <p class="section-tag">Kindly Respond</p>
  <h2 class="section-title">Will You <em>Join Us?</em></h2>
  <div class="divider" style="margin-top:1.5rem;"></div>
  <p style="font-family:'Cormorant Garamond',serif;font-style:italic;color:rgba(253,246,236,0.5);margin-top:1rem;font-size:1.1rem;">Please RSVP by 10 June 2026</p>

  <form class="rsvp-form" onsubmit="handleRSVP(event)">
    <div class="form-row">
      <input class="rsvp-input" type="text" placeholder="Your Name" required/>
      <input class="rsvp-input" type="email" placeholder="Email Address" required/>
    </div>
    <div class="form-row">
      <select class="rsvp-input">
        <option value="">Attending?</option>
        <option value="yes">Joyfully Accept</option>
        <option value="no">Regretfully Decline</option>
      </select>
      <input class="rsvp-input" type="number" placeholder="Number of Guests" min="1" max="6"/>
    </div>
    <textarea class="rsvp-input" rows="3" placeholder="A message for the couple (optional)" style="resize:none;"></textarea>
    <button class="rsvp-btn" type="submit">Send RSVP</button>
    <p class="rsvp-success" id="rsvpSuccess">🌸 Thank you! We can't wait to celebrate with you.</p>
  </form>
</section>

<!-- ══════════ FOOTER ══════════ -->
<footer>
  <div class="divider" style="margin-bottom:2rem;"></div>
  <p class="footer-names">Rajat <span class="amp">&amp;</span> Priyanka</p>
  <p style="font-family:'Cinzel',serif;letter-spacing:0.2em;font-size:0.7rem;color:var(--gold);margin-top:0.5rem;">25 · JUNE · 2026</p>
  <div class="divider" style="margin-top:1.5rem;"></div>
  <p style="font-family:'Cormorant Garamond',serif;font-style:italic;color:rgba(253,246,236,0.4);font-size:1.05rem;margin-top:1.5rem;">"A great marriage is not when the 'perfect couple' comes together — it is when an imperfect couple learns to enjoy their differences."</p>
  <p class="footer-note">Made with ❤️ for Rajat &amp; Priyanka's Wedding</p>
</footer>

<script>
  // ── COUNTDOWN ──
  const weddingDate = new Date('2026-06-25T19:00:00');

  function updateCountdown() {
    const now = new Date();
    const diff = weddingDate - now;
    if (diff <= 0) {
      document.getElementById('cd-days').textContent = '0';
      document.getElementById('cd-hours').textContent = '0';
      document.getElementById('cd-mins').textContent = '0';
      document.getElementById('cd-secs').textContent = '0';
      return;
    }
    const days  = Math.floor(diff / 86400000);
    const hours = Math.floor((diff % 86400000) / 3600000);
    const mins  = Math.floor((diff % 3600000) / 60000);
    const secs  = Math.floor((diff % 60000) / 1000);
    document.getElementById('cd-days').textContent  = String(days).padStart(2,'0');
    document.getElementById('cd-hours').textContent = String(hours).padStart(2,'0');
    document.getElementById('cd-mins').textContent  = String(mins).padStart(2,'0');
    document.getElementById('cd-secs').textContent  = String(secs).padStart(2,'0');
  }
  updateCountdown();
  setInterval(updateCountdown, 1000);

  // ── RSVP ──
  function handleRSVP(e) {
    e.preventDefault();
    const btn = document.querySelector('.rsvp-btn');
    btn.textContent = 'Sending…';
    btn.disabled = true;
    setTimeout(() => {
      document.getElementById('rsvpSuccess').style.display = 'block';
      btn.style.display = 'none';
    }, 900);
  }

  // ── Fade in on scroll ──
  const observer = new IntersectionObserver(entries => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        e.target.style.opacity = '1';
        e.target.style.transform = 'translateY(0)';
      }
    });
  }, { threshold: 0.15 });

  document.querySelectorAll('.event-card, .gallery-cell, .countdown-block').forEach(el => {
    el.style.opacity = '0';
    el.style.transform = 'translateY(30px)';
    el.style.transition = 'opacity 0.7s ease, transform 0.7s ease';
    observer.observe(el);
  });
</script>
</body>
</html>